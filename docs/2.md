## 第13大节提交内容详细指导

根据你的服务器已部署完成（47.99.126.39），以下是每项截图的具体操作步骤：

---

### 1. 部署验证截图（ECS方案）

**① SSH连接服务器成功截图**
```bash
# 在VSCode终端或PowerShell执行
ssh root@47.99.126.39
# 截图显示: root@iZrdrgm1podlylZ:~#
```

**② PM2状态截图**
```bash
ssh root@47.99.126.39 "pm2 status"
# 截图显示 sleep-care 状态为 online
```

**③ Nginx配置截图**
```bash
ssh root@47.99.126.39 "cat /etc/nginx/sites-available/sleep-care"
# 截图显示配置内容（proxy_pass http://localhost:3000）
```

**④ 浏览器访问健康检查截图**
- 打开浏览器访问: `http://47.99.126.39/`
- 截图显示返回的JSON: `{"code":0,"message":"Sleep Care Backend is running"...}`

**⑤ MySQL表截图**
```bash
ssh root@47.99.126.39 "mysql -u root -p sleep_care -e 'SHOW TABLES;'"
# 输入MySQL root密码后截图显示7张表
```

---

### 2. 数据持久化验证截图

**① 小程序注册新账号**
- 微信开发者工具打开小程序
- 点击"注册"按钮，填写新手机号（如 `13900001111`）
- 截图注册成功页面

**② MySQL查询该用户**
```bash
ssh root@47.99.126.39 "mysql -u root -p sleep_care -e 'SELECT user_id, phone, nickname FROM users WHERE phone=\"13900001111\";'"
# 截图显示查询结果
```

**③ 重启服务器**
```bash
ssh root@47.99.126.39 "pm2 restart sleep-care"
# 截图显示重启成功
```

**④ 重启后登录成功**
- 小程序用新注册的账号登录
- 截图登录成功页面

**⑤ 再次查询MySQL数据**
```bash
# 再次执行第②步的查询命令
# 截图显示数据仍然存在
```

---

### 3. 环境变量配置截图

```bash
ssh root@47.99.126.39 "cat /opt/sleep-care/backend/.env"
# 截图显示:
# DATABASE_TYPE=mysql
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_DATABASE=sleep_care
```

---

### 4. 小程序运行截图

**① 首页截图**
- 小程序登录后首页显示睡眠数据
- 截图显示评分、时长等数据（从线上API拉取）

**② 注册/登录页面截图**
- 截图登录页面（手机号+密码输入框）

**③ 真机预览截图**
- 微信开发者工具点击"预览"生成二维码
- 手机扫码打开小程序
- 截图手机运行界面

---

### 5. 医生端Web运行截图

- 浏览器打开: `http://47.99.126.39/doctor.html`
- 截图登录页面
- 用医生账号登录（`13900000100` / `123456`）
- 截图患者列表页面

---

### 6. 演示视频截图

**录制视频步骤：**
1. 使用OBS Studio或Windows自带录屏（Win+G）
2. 演示内容：
   - 小程序登录 → 首页睡眠数据 → 医生授权
   - 医生端Web登录 → 患者列表 → 查看报告 → 填写干预建议
3. 保存为 `docs/demo.mp4`（时长2-4分钟）

**截图要求：**
- 视频播放器打开 `docs/demo.mp4`
- 截图显示视频时长（如 02:35）

---

### 7. Git提交历史截图

```bash
# 在项目目录执行
git add .
git commit -m "final: 实训项目最终交付"
git tag v1.0-final
git log --oneline -5
# 截图显示提交记录和标签
```

---

### 8. 项目文件结构截图

- VSCode资源管理器展开 `docs/` 目录
- 截图显示: `demo.mp4`, `migration.sql`, `api.yaml`, `硬件对接方案.md`

---

### 快速执行脚本

我可以帮你一键执行所有SSH命令，你只需要截图：

```bash
ssh root@47.99.126.39 "
echo '=== PM2 Status ===' && pm2 status
echo '=== Nginx Config ===' && cat /etc/nginx/sites-available/sleep-care
echo '=== MySQL Tables ===' && mysql -e 'SHOW TABLES;' sleep_care
echo '=== Environment ===' && cat /opt/sleep-care/backend/.env
"
```

需要我执行这个命令吗？